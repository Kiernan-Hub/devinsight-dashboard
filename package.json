{
  "name": "devinsight-dashboard",
  "version": "0.6.0",
  "private": true,
  "type": "module",
  "description": "Live performance telemetry pipeline for the Godot game Ascent.",
  "scripts": {
    "test": "node --test lib/*.test.mjs scripts/*.test.mjs",
    "test:godot": "godot --headless --path godot --script tests/test_logger_queue.gd && godot --headless --path godot --script tests/test_benchmark_model.gd",
    "check:regression": "node scripts/check-regression.mjs",
    "check:logger-sync": "node --test scripts/logger-sync.test.mjs",
    "serve": "python3 -m http.server 8000",
    "benchmark": "node scripts/run-benchmark.mjs",
    "benchmark:update-baseline": "node scripts/run-benchmark.mjs --update-baseline"
  },
  "engines": {
    "node": ">=20"
  }
}
