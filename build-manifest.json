{
  "$comment": "Declares which build the code in this commit produces, and which build it must be measured against. scripts/check-regression.mjs verifies that godot/system_logger.gd actually stamps build_version with the value below, then compares telemetry for exactly these two versions. Bump build_version in BOTH this file and godot/system_logger.gd when you cut a new build.",
  "build_version": "0.6.0",
  "baseline_version": "0.3.0",
  "min_samples": 30,
  "regression_threshold": 0.1,
  "warning_threshold": 0.05
}
